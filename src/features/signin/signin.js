import React from 'react';
import PropTypes from 'prop-types';
import { Button, Typography } from 'antd';
import { Paragraph, Title } from '../../components/styled/Typography';

const Signin = props => {
  return <Paragraph>hello</Paragraph>;
};

Signin.propTypes = {};

export default Signin;
